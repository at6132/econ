"""Infrastructure — physical movement, logistics, energy, roads.

Submodules:
  * ``realm.infrastructure.movement``       — Inter-region/inter-plot shipping
  * ``realm.infrastructure.plot_logistics`` — On-plot logistics, harvest dispatch
  * ``realm.infrastructure.power_grid``     — Regional electricity market + brownouts
  * ``realm.infrastructure.road_connectivity`` — Road access gates for production
  * ``realm.infrastructure.roads``          — Road segments, tolls, build orders
  * ``realm.infrastructure.route_operators`` — Route operator NPCs (shipping market)
"""
